# Residual upload/retry candidate disposition

Compared untouched mixed source and `release/business-onboarding-fixes` (`de9b734`) with main428cdbc on10September2026. Never merge that branch wholesale. Released source remains authoritative; backup and historical branches retained. No media runtime changes are introduced in this batch.

| Residual change | Disposition and evidence needed |
| --- | --- |
| Unresolved location removal, canonical empty-array persistence; verified-token refresh; navigation; save focus/status; optional-image eligibility | Already in main viaPR25. Source comparisons for auth.js, locations.js and businessCompletion.js are identical. Retired from the pending integration scope, not deleted from backup. |
| Logo control CSS and versioned image display | Released viaPR25; older broad replacements would replay old Services CSS. Preserve current authoritative presentation. |
| Legacy gallery rules allowance and canonical numeric-slot deletion | Released viaPR26; never overwrite with broad source. Production legacy removal/reopen confirmed; canonical deletion remains emulator-verified. |
| uploadBytesResumable +45s cancel | Separate unverified transport change. Fixed45s can reject supported images on slow mobile connections. Need real Storage success/slow progress/stall/cancel, token expiry, metadata/generation retention and late completion tests. Browser timer is not server cancellation proof. |
| 30s media callable deadline | Separate: a deadline does not cancel server finalization. Need dropped-response after commit, retry of exact session/generation and one attachment; test beyond30s before selecting a deadline. |
| pendingByBusiness content/session map | Separate, not merge-ready. Retains prepared/uploaded session after ambiguous finalization, but keyed only by business/file; no session-expiry or auth-change cleanup, no reload persistence. Awaited creation can race before map insertion. Require expired/consumed sessions, logout/account switch, overlapping selections, reload, same/different contents, partial multi-file success and late commit tests. Preserve backend generation/metadata protections. |
| 10/15s reads and refresh error after attachment | Useful distinction, unverified. withTimeout already exists; do not add a duplicate. It does not cancel reads. Need commit-success/read-failure UI, retry refresh without upload, edit-state preservation and late read result tests. |
| mediaPresentationIncomplete flag | Separate presentation signal, not a transport fix. Need mixed canonical/legacy galleries, missing/inaccessible objects, all supported image formats and partial URL resolution; successful committed image must not trigger duplicate upload. |
| Retry despite currently full gallery | Coupled to safe session reconciliation, not an independent UI fix. Need final-slot commit with lost response, reload/full gallery and stale selection; preserve remaining images and never bypass backend capacity. |
| Prevalidate entire batch and unify >=5MiB boundary | Canonical upload already rejects >=5MiB in current main; basic allowed-type/size checks exist. Eager batch validation changes partial-success behaviour. Keep exact boundary check, do not replay the refactor without unit boundary/real rules tests and an explicit batch-error UX. |
| More actionable error mapping and refresh action | Useful only with confirmed phase/code; do not map every unknown finalizer failure to metadata-save failure. Test permission/status/unauthenticated/timeout distinctions and no duplicate retry after successful attachment. |
| Emulator startup/config changes | Existing main already has protected test isolation. Do not import old envDir/preview-guard overrides; current tests and guards are authoritative. |
| Cloud harness and bucket portability (`investigation/gallery-isolated`,3111da9) | Keep test infrastructure separate. Historical branch includes stale full website/backend source: cannot merge wholesale. Config assumes holalocal-test-* and *.firebasestorage.app; runner/setup/trigger runtime dependencies need fresh extraction/verification against main. Copy production IAM/rules wholesale is prohibited. No cloud run evidence yet. |

Original incident: two legitimate owner uploads later succeeded and persisted; this proves ordinary upload/attachment/display for those attempts, not recovery from the earlier failure. The original production failure code remains unconfirmed. The emulator `media-forbidden-metadata` finding must be compared with real Storage metadata, generation and SDK/emulator versions; do not weaken validation. Needs-changes eligibility allowed successful uploads. Pending/Active maintenance restriction is a separate product proposal, not a cause established for the earlier attempt.

Next concrete reliability evidence: an isolated real Storage/callable run from current main, then controlled loss of finalization response AFTER attachment and a retry with the same session. Record sanitized action/code, generation, reference/object counts before/after, reload and preserved other images. No production test account/business is assumed eligible; no status changes or synthetic production records. Local emulators can cover deterministic races now, but cannot establish real Storage metadata parity. No unproven improvement is ready to merge on the strength of happy-path production uploads.

No obsolete runtime needed deleting in the new integration worktree: it was created from current main, where the retired replacements are already absent or superseded. The original mixed144 files and recovery snapshot remain byte-for-byte preserved.
