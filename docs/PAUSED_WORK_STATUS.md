# Current paused-work and launch status

Baseline verified 12 September 2026: main and the successful Vercel production deployment use `87d5fbd5de5f1ba6b7f10dbab11e9655c307c219` (PR43). This document supersedes the contradictory pre-rollout status statements retained in [the historical inventory](history/PAUSED_WORK_STATUS_PRE_LEGAL_RECONCILIATION.md). Recheck source and cloud state before release.

## Integrated and preserved

PR25–42 retain released onboarding/location/save, media display/deletion, Services, Profile/Business, Admin and mobile tap-highlight work. PR33–36 reviews/translation/operations code was merged and the scoped disabled backend deployed. Do not replay old candidates. PR37's monitoring-generator correction remains an open draft; its cloud filter correction is already configured.

The original mixed workspace and recovery backup remain untouched. The historical 144 files were 45 modified tracked plus 99 untracked files, distinct from the 598-file recovery fingerprint. This update does not claim a fresh rehash of all backup files.

## Current preparation

- PR43 is merged and deployed: automatic Firebase Analytics initialization and its unused client module are removed. Business usage-count callables and Cloud Monitoring remain intact. PR44 is still unpublished.
- The legal candidate updates authoritative Privacy/Terms components, common contents navigation, original styles and all 17 translations. It distinguishes accepting Terms from acknowledging reading Privacy. Existing policy versions remain 1.0; no forced consent migration. Pages explicitly identify the draft update as not effective.
- Operator/contact legal particulars, an age decision, provider/transfer details and legal assessment remain publication blockers or unresolved factual requirements. A private checklist outside Git records them; no invented details or bracketed public placeholders. Do not publish this incomplete candidate.
- Original upload/retry work remains unverified and separate. Successful ordinary uploads do not establish retry recovery. Approved-business maintenance remains a proposal; no new moderation policy.

## Read-only launch reconciliation

Individual Function and Cloud Run readbacks show reviews, public/private translation providers, retention and erasure-recovery controls absent/default-disabled. Existing business registration, submission and approval remain independent of review gates.

The 17 browser review callables retain the authorized invoker-check correction. Scheduled workers retain authenticated access restricted to the intended runtime service account. Recent natural Scheduler attempts report success. No manual cleanup or production test records were created in this reconciliation.

Monitoring replacement delivery was user-confirmed received; do not resend. Craig receives alerts at hello@holalocal.es, Europe/Madrid, checks when available, with no staffed hours, response guarantee or backup. Ten operational policies are enabled; two completion-absence policies await activation and their first completion series. The previously approved EUR30 Translation alert is not a cap; no new quotas authorized.

Runtime account: `1097633279895-compute@developer.gserviceaccount.com`. No direct Translation role was found; effective permissions are still unverified. The earlier diagnostic Job failed before startup and made zero provider requests. Offline examination now verifies a working Node v20.20.2 binary in the selected runtime base, but not a complete diagnostic container. A corrected image/startup check and separately approved bounded runtime probe remain necessary. Local ADC success is not runtime access evidence.

## Verification and release checkpoint

Targeted legal/consent tests, all-language public-page browser checks and real emulator customer/admin review journeys are recorded under the durable legal-review-launch evidence directory. Translation component browser checks use fake transport, not a provider. No legal/native-language certification is claimed.

Before release: finish candidate checks and preview; resolve private publication requirements; prepare exact source/deployment scope and approval for the corrected runtime probe; verify deployed translation/cache access; then seek approval for the concrete activation and rollback sequence. Keep reviews, translation, retention and recovery closed until required checks pass. No production merge, Firebase/IAM changes or provider requests are authorized by this preparation.

### Candidate verification, 11 September 2026

- 78 startup/emulator safety tests for the separate Analytics removal; 16 targeted legal/consent tests pass.
- 68 real public-page checks (17 languages × two pages × mobile/desktop), keyboard anchors, reload and registration links pass.
- Real emulator consent recovery: all 17 languages, unchecked acknowledgments, acceptance persistence, unchanged 1.0 versions.
- Real emulator customer/admin flow: submit, moderation/publication, edit, report, lost-response retry, quota handling, overdue resolution, withdrawal and reload pass. Synthetic data only.
- Translation component checks: all 17 languages, keyboard Show original, preserved name, language-race/revision/failure handling pass using fake transport. Not real-provider acceptance.
- Lint, locale parity and fresh production build pass. Bundle check fails at **207.62 kB gzip**, unchanged 200 kB limit. Previously recorded main baseline is **205.61 kB**: the failure predates this candidate, but this candidate adds about **2.01 kB**. No budget increase or bypass.
- No Functions, shared contracts, rules, indexes, production gates or credentials changed. Original CSS/component implementation updated directly; obsolete single-email-only rendering and automatic Analytics client removed, no replacement pages or override layer.

Publication remains blocked; these automated results are not legal or native-speaker editorial approval. Draft PR previews do not authorize production publication or activation.

## Separate-batch follow-up, 11 September 2026

PR43 remains independently mergeable at 543932cb820e64bc7d4360975d5785761e974417 against unchanged main7ebc477. Its only runtime change removes automatic Firebase Analytics initialization and the unused module. Auth/AppCheck, business services/insights, Functions and operational monitoring source are unchanged. An independent exact-source checkout passes78 startup tests plus81 focused Auth/AppCheck/business/insights/media tests, lint and fresh build. Vercel preview succeeds. Website-only release is recommended for approval; keep PR44 out. Fresh bundle205.45kB still fails unchanged200kB limit. Main205.61kB was reproduced using identical dependencies. Do not bypass any required check. Rollback source is7ebc477; returning to it would restore automatic Analytics initialization.

PR44's first candidate207.62kB included legal-only launch notices for all17 languages in an eager shared module. Moving non-English notices into the existing lazy legal module preserved every legal-content object and saved1.32kB (206.30kB). Verified business usage-count disclosure then added0.07kB: final206.37kB, net1.25kB reduction and0.76kB over main. Remaining added startup content is chiefly English disclosures/consent wording and contents rendering; existing eager English architecture retained. No limit increase or unrelated optimization.16 targeted tests,68 language/page/viewport cases, lint, locale parity and fresh build pass after the change.

Business view/contact counts use a separate active recordBusinessInsight callable and browser session token; PR43 preserves them. PR44 now discloses them in all17 languages. A professional assessment of their storage/consent/legal basis remains distinct from code verification. Removing Firebase Analytics does not erase prior collected records, if any.

Runtime preparation: the selected base's actual Node20 binary imports the authoritative adapter/service and passes8 existing offline cache/provider tests with fake providers. A corrected Dockerfile now specifies the verified executable and requires a network-disabled module-import smoke. No complete image build or real runtime probe has passed. The previous single Job execution approval was consumed; unused Translation allowance does not authorize another attempt. A one-request74-character EN→ES Job under the deployed runtime account can verify real provider access and cache logic using a memory-only fixture, but cannot prove deployed Firestore/cache or public-callable integration. The latter needs separately approved eligible genuine content or an isolated cloud environment. No gates, IAM, quotas, provider calls or notifications changed.

## Current release and preparation — 12 September 2026

PR43 was explicitly approved, marked ready and merged at87d5fbd5de5f1ba6b7f10dbab11e9655c307c219. Its treeb63dc693d93b156d8e50b091720947f7f34498bc exactly matches reviewed head543932cb820e64bc7d4360975d5785761e974417. Vercel Production deployment6402446520 succeeded: https://hola-local-c422m3sfv-hello-8446s-projects.vercel.app . The unique deployment URL is protected; public verification used holalocal.es, www.holalocal.es and hola-local.vercel.app, which serve/redirect to the same new entry asset index-Ck7ORKy6.js.22 mobile/desktop production route checks passed with no automatic Analytics requests and no business writes attempted. Auth forms and signed-out guards passed; genuine signed-in sessions require the operator's manual check. No Firebase deployment or activation change. Rollback source7ebc477 would restore automatic Analytics initialization.

PR44 retargeted to current main and remains draft/unpublished. Craig confirmed he operates HolaLocal personally, full legal name Craig Evans; HolaLocal is the platform name. Both authoritative legal pages now say this in all17 languages. The postal contact address is the next single pending question; no company/tax status inferred.17 targeted tests,68 responsive browser checks, lint, locale parity and fresh build pass. Latest bundle206.43kB vs current PR43/main205.45kB; limit200kB unchanged. Earlier205.61kB comparisons refer to pre-PR43 main, not the latest production source.

The corrected runtime diagnostic is now a complete local OCI image, digest sha256:448eb8b902342ea2e7cc04f03942f9733641a3fc9473143366cfddc6bd04d972. It uses the pinned Google Node20 base and reviewed backend source/lockfile with the existing contract packager. The old application artifact was not found in Artifact Registry, so the image is built from source; it is not represented as the recovered deployed image.78 authoritative backend/shared files match byte-for-byte. All image blobs verified; full filesystem plan/self-test startup passed as UID/GID33 with no external network interfaces. Fake-provider tests cover bypass/cache/revision/removal/failure, with zero paid requests. This supersedes earlier binary-only packaging limitations.

No image has been uploaded and no new Job run. Proposed one-task/zero-retry/60-second runtime test requires separate approval: one74-character EU request under the actual runtime account, memory-only cache fixtures, no production review records. Proposed total allowanceUSD0.03 before credits/tax/FX includes Translation, Job, image storage and minimal logs. Existing repository scanning is disabled; do not enable it. Preserve results then delete only the newly created diagnostic Job/image. Runtime permissions and deployed Firestore/cache acceptance remain unverified; no grant, public activation or additional paid test is authorized by this preparation.
