# Reviews activation readiness — current checkpoint, 12 September 2026

This supersedes the completed PR33–36 merge/deployment instructions in earlier Git revisions. Those batches are already integrated and deployed disabled. Do not replay the historical20-Function deployment. This is preparation, not authority to activate, deploy, grant IAM or run another paid test.

## Source and publication

Verified main and READY Vercel production: `2e49239b8f96bb5d9aee55760142edec136d7bb2`. PR45 Analytics remains consent-enabled; preserve its flag, visitor choice, ad restrictions, public-route allowlist and existing evidence. PR44 is being reconciled against that baseline, and remains an unpublished draft. Current candidate head/tree and preview are recorded in PR44 and the durable legal-review-launch result, avoiding a self-referential commit hash here.

`functions/` and `shared/` are unchanged from reviewed diagnostic source `543932cb820e64bc7d4360975d5785761e974417`. Prior emulator review/moderation, concurrency, quotas, report retention, account erasure and browser translation results are carried forward for unchanged code. All17-language legal/Analytics integration checks run against the combined website. Automated checks do not establish legal or native editorial approval.

Craig Evans is confirmed as individual operator; postal address remains unresolved privately and is not repeatedly requested. Account age18 is now approved and reflected in draft Terms. Registration now requires an unchecked18+ self-confirmation before the website starts account creation. It is not age verification or a new backend access rule. Public browsing and existing accounts have no new restriction; no birth date or additional stored personal field is introduced. No forced reacceptance, new consent version or account migration is introduced here. Outstanding provider/retention/legal particulars and publication/version handling remain in the private checklist outside Git. No incomplete-page publication is recommended.

## Runtime test: exact approval received; execution pending

The old one-run Job failed before application startup because its Functions image lacked the Node runtime supplied separately by Functions. Zero Translation calls; temporary Job removed. Its single-run approval is consumed. No effective runtime permission conclusion follows from that failure or from local ADC success.

Corrected complete local image: `sha256:448eb8b902342ea2e7cc04f03942f9733641a3fc9473143366cfddc6bd04d972`. Entrypoint SHA256 `9e13e89eb98a3a113ec9eed5892b81182813c1759c12757ce360d53b4e35a529`. Full offline startup and fake-provider/cache tests passed. No upload or corrected execution is recorded. The latest user instruction approves one exact corrected execution; read cloud history first and never run twice. Exact executable/configuration and evidence remain in durable runtime-image/APPROVAL_PACKAGE.md and PROPOSED_JOB.json.

Approved one-run scope: upload only this image to existing europe-west1 gcf-artifacts; create/run/remove one temporary `holalocal-review-translation-probe` Job using `1097633279895-compute@developer.gserviceaccount.com`. One task, parallelism1, retries0,60s timeout,1vCPU/512MiB. One74-character synthetic English-to-Spanish request, `translate-eu.googleapis.com`, `europe-west1`,10s deadline, no fallback. No real reviews, Firestore writes or policy/gate change. Stop on collision, missing access or first failure. Check metadata identity plus `cloudtranslate.generalModels.predict` and `serviceusage.services.use`; never add grants automatically.

Approved USD0.03 total allowance before credits/tax/currency conversion: Translation at mostUSD0.01 and Job/image/logs at mostUSD0.02. Prior documented estimate: TranslationUSD0.00148, one-minute Job minimum aboutUSD0.00114, temporary image storage aboutUSD0.00134 for24h, plus small logs/startup differences. These are estimates, not a billing cap or invoice. Preserve results then delete only the Job and newly uploaded diagnostic image; verify absence. Existing operator permissions must suffice; no service-account key or IAM grant.

Success proves the actual account can call the provider and the authoritative cache algorithm works with an in-memory database. It does not prove deployed Firestore cache persistence or translation quality.

## Current cloud-evidence limitation

GitHub/Vercel readbacks succeeded this session. Fresh Firebase/Google Cloud inspection failed because gcloud could not refresh its credentials. Reauthenticate with `gcloud auth login` for holalocal-491c9, then recheck controls, identities, indexes, worker IAM/natural logs and alerts read-only. Historical successful checks remain evidence, not a fresh assertion about today's cloud state. No cloud mutations were attempted.

Prior records show17 browser callables with invoker IAM checks disabled as approved, Auth/application/App Check preserved, workers authenticated to the intended Scheduler account, and separate review/provider/retention/recovery controls absent/default-disabled. Business registration/submission/approval are independent. Monitoring delivery was confirmed received; no resend. Craig checks hello@holalocal.es when available in Europe/Madrid, with no staffed hours or backup. Preserve the EUR30 Translation alert and all existing quotas. Two completion-absence alerts need their enabled-worker completion series before arming; existing actionable policies remain separate from this integration.

## Controlled deployed-cache acceptance still needs a decision/approval

Smallest positive check needs a genuine eligible published review, with the author's intended public content and approval for bounded translation, OR a separately authorized isolated cloud dataset. No public synthetic record or genuine-record modification is covered here. Bound characters/cost before one English-to-Spanish translation, repeat/reload to verify cache without another provider call, and verify revision invalidation only after a genuine intended edit. Check private/pending/unavailable revisions remain inaccessible; use emulator evidence for destructive scenarios. If no eligible record exists, explicitly decide whether first-genuine-review acceptance may occur after controlled launch; do not silently label full cloud cache coverage passed.

## Proposed deployment order after blockers and final approval

1. Capture then-current main, source tree, Vercel deployment/env and scoped Function/Cloud Run revisions/env/IAM plus Scheduler state. Verify closed controls and additive indexes ready. No Firebase deploy is needed for PR44 wording itself.
2. Resolve legal publication/version and age-enforcement dependencies; prepare the exact final website head, normal required checks and preview. Publish only with approval. Review/disclosure launch wording must track actual enabled state; current draft correctly says unavailable and cleanup not running. Do not switch that wording prematurely or leave it false after activation.
3. With reviews closed, configure only `translatePublishedCustomerReview`: `CUSTOMER_REVIEW_TRANSLATION_PROVIDER=google_cloud`; preserve compiled EU endpoint/location, timeout and metadata/revision protections. Verify runtime access first. Do not deploy unrelated media, messaging, rules or indexes.
4. Separately approve `ACCOUNT_DELETION_RECOVERY_ENABLED=true` only on `recoverAccountDeletions`. Observe its next authenticated natural execution and safe counts. Existing authorized erasures may complete irreversibly; no new test erasures. Initialize the completion series before enabling its absence alert.
5. Separately approve `CUSTOMER_REVIEW_RETENTION_ENABLED=true` only on `sweepResolvedCustomerReviewReports`, after an expiry/status/count preflight. Observe natural completion and poison-record isolation; zero eligible records is not positive deletion evidence. Initialize its completion series before the absence alert.
6. Set `CUSTOMER_REVIEWS_ENABLED=true` only on the17 browser exports listed below. Stop and close the affected set on partial enablement. Preserve existing browser invoker-check correction and worker authenticated access; Firebase deployment may attempt rejected allUsers grants, so verify the documented supported invoker-check configuration rather than silently adding IAM or changing ingress.
7. Only when backend acceptance passes, set Production `VITE_CUSTOMER_REVIEWS_ENABLED=true` and release the exact approved website source through Vercel. Preserve Production Analytics=true, all other settings, review quotas and EUR30 budget. Verify aliases, assets, guarded /my-reviews, /admin/customer-reviews and /admin/customer-review-reports, and authorized genuine acceptance. No production moderation/deletion action without its own authority.

The17 browser exports are: submitCustomerReview, editCustomerReview, withdrawCustomerReview, approveCustomerReview, rejectCustomerReview, removeCustomerReview, listPublishedCustomerReviews, getOwnCustomerReview, listOwnCustomerReviews, listCustomerReviewModerationQueue, getCustomerReviewModerationCase, submitCustomerReviewReport, listCustomerReviewReports, getCustomerReviewReport, resolveCustomerReviewReport, getCustomerReviewRatingSummaries, translatePublishedCustomerReview. Recovery and retention are two additional separately controlled workers. `finalizeAccountDeletion` remains deployed unchanged; no new deployment of it is proposed by this website reconciliation.

Pass criteria: source identity exact; no unexpected environment changes; indexes ready; normal browser preflight/handler access with correct auth/gate denial; only intended Scheduler identity can invoke workers; closed gates produce no cleanup writes; runtime EU request succeeds within limits; cache evidence honestly scoped; enabled worker completion/alerts healthy; publication/visibility and private-data restrictions preserved. Any failed critical stage blocks later activation.

## Rollback and limitations

Before any approved action capture fresh rollback artifacts. Current website rollback reference is READY `dpl_9jBtngoyKbtUEX7Aq343rbQmP2iZ`, commit2e49239, https://hola-local-5anbm53qx-hello-8446s-projects.vercel.app. Preserve Analytics consent functionality when reverting legal/reviews work.

Close website/review/provider and each worker control to stop new work; in-flight work may complete. Restore only captured scoped revisions/env/IAM, not old unrelated settings. Keep monitoring and checkpoints. Completed erasure/retention deletions cannot be undone, and provider usage remains billable. No synthetic records, customer-data changes, IAM grants or destructive production tests are authorized. Functions/Cloud Run, Scheduler, Firestore, Monitoring and Translation may incur charges; Translation's alert is not a hard cap. Known bundle-budget failure remains visible and required checks may not be bypassed.
