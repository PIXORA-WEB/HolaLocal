# Coordinated launch package — PR33 → PR34 → PR35 → PR36

This is the current release authority. Earlier deployment scopes, budget proposals, staffing placeholders and launch checklists are historical where they conflict with this document. No merge, deployment, IAM mutation, operational alert creation or activation is authorised by this document.

## Verified source and order

Main remains 428cdbc08617d53e3498ffb5593e7200807dfa7c. PR33 head 5c3f90c0f27d5c10fe26e552fb706659a746b5e0; PR34 head 5c12f4273e1707a6dadc40ad09a31e3734cff29a; PR35 head 73e6e3a423b9ab4dd12f81b01c78bf64dccd1378; PR36 runtime head 66e228da955eb8360a4d3349b1088f1e73f414ac. All open, correctly stacked and mergeable at inspection. PR35/36 remain draft. The disclosure follow-up changes only website privacy content, submission notice, shared localized copy and affected tests. Functions/shared remain byte-identical to the tested runtime source. Final subtree hashes are recorded in scripts/reviews-operations/launch-package.json. The final commit/full tree is recorded in the PR36 approval comment; do not equate a GitHub merge commit with its source tree.

Merge using merge commits, never squash/rebase: #33 then retarget #34 to main/recheck, #34 then retarget #35/recheck, #35 then retarget #36/recheck, finally #36. Before each merge compare expected reviewed head and required checks; stop on unexpected differences. Automatic Vercel deployments between merges are safe only with VITE_CUSTOMER_REVIEWS_ENABLED absent/false and existing backend gates closed. Final main tree must equal the final approved PR36 tree before Firebase deployment. Build only from that source. Preserve production environment and original paused workspace/backups. Review UI/translation stay invisible/inactive throughout disabled merges; account recovery is not created by website deployments. Business registration/submission/approval remain independent and permitted under their existing checks.

## Verification carried forward without needless repetition

On exact runtime candidate: 358 backend unit passes, 29 protected emulator skips; separate 36 real Firebase emulator passes, including erasure concurrency/current admin/ownership checks, retention poison-record progress and private progress rules. Five offline monitoring checks, Functions lint and clean deployment artifact pass. The shared contract and Functions trees remain unchanged; the affected website source has fresh disclosure verification recorded below. Previous browser coverage includes 17 selected UI languages, originals, stale translation/revision handling, mobile/desktop, escaping and failure display. Real-provider evidence: 16 sequential genuine EU translations, 1,184 synthetic characters, all API responses successful under user ADC. This is not native quality approval or runtime service-account proof. Subtree hashes, scope and ancestry are checked locally; unchanged backend suites are not repeated for the website disclosure.

## Only remaining launch blockers

1. **Runtime access/cloud deployment acceptance:** actual new callable identity is unknown until deployed; existing account is 1097633279895-compute@developer.gserviceaccount.com. The fresh effective-permission check returns SERVICE_DISABLED for policytroubleshooter.googleapis.com, not a confirmed IAM denial. Enable this diagnostic API only after approval, then require effective GRANTED for cloudtranslate.generalModels.predict and serviceusage.services.use on the actual deployed identity. UNKNOWN/deny stops activation. If missing, present the exact minimum role/binding separately; no blanket grant is included. No keys, allUsers, organisation-policy or ingress changes.
2. **Operational monitoring and receipt:** email destination exists; the one authorised test log was accepted but inbox receipt is unconfirmed and no delivery record proves receipt. Craig must confirm the existing message arrived (including spam search); do not resend automatically. Prepare/create three scoped log metrics and twelve reviewed policies from the existing generator, validating API schemas/descriptor units first. Only actionable policies email Craig; overlapping diagnostics have no email channel, 24-hour log notification rate limit, no renotify. No fixed hours/response guarantee/backup. Completion absence alerts arm only after their first real series; Scheduler success alone is not cleanup proof.
3. **Scheduled safeguards:** verify both worker identities/permissions, authenticated Scheduler targets and default-off/no-write runs, then read-only candidate/expiry/checkpoint preflight. Stop on malformed expiry, unexplained backlog or unauthorised erasure state. Recovery only resumes previously authorised work; five-retry bound/current authoriser/ownership/leases remain. Blocked/exhausted/pending initial approvals still need Craig's attention. Retention isolates records and collections; irreversible cleanup must not begin before checks/monitoring pass.
4. **Explicit controlled activation and cost acceptance:** EUR30/month Translation alert is configured, EUR billing and INCLUDE_ALL_CREDITS confirmed. No daily/minute quota changes are approved or proposed as part of this release. Budget is a delayed alert, not a cap; activation approval must acknowledge existing provider quotas and possible overspend. No extra quota decision is manufactured as a prerequisite. Full runtime provider/cache path remains unverified; choose the bounded acceptance approach below and approve necessary paid/test actions. Final public activation requires a new concrete checkpoint after all readiness checks pass.

Native-speaker polish/real-listing visual feedback are follow-ups. Original upload/retry incident and approved-business maintenance are separate. Existing display-name, 5 review/edit +10 report limits, 90-day resolved retention and 7-day overdue decisions remain approved; do not ask again.

## Exact disabled deployment after approval

Record rollback: current Vercel deployment ID/aliases/commit, all existing scoped Function revision/source/env/identity/ingress/invoker-check configurations, Scheduler target/OIDC/IAM, indexes, and monitoring resources. Capture only sanitized records; preserve secrets privately. Current website baseline is main above; capture the actual Vercel rollback deployment ID at execution rather than inventing one.

Keep all three backend flags false/unset, provider absent/disabled and website flag false. Deploy exactly 20 exports (17 review callables + existing finalizer + existing retention worker + new recovery worker):

```sh
firebase deploy --project holalocal-491c9 --only functions:submitCustomerReview,functions:editCustomerReview,functions:withdrawCustomerReview,functions:approveCustomerReview,functions:rejectCustomerReview,functions:removeCustomerReview,functions:listPublishedCustomerReviews,functions:getOwnCustomerReview,functions:listOwnCustomerReviews,functions:listCustomerReviewModerationQueue,functions:getCustomerReviewModerationCase,functions:submitCustomerReviewReport,functions:listCustomerReviewReports,functions:getCustomerReviewReport,functions:resolveCustomerReviewReport,functions:getCustomerReviewRatingSummaries,functions:translatePublishedCustomerReview,functions:finalizeAccountDeletion,functions:sweepResolvedCustomerReviewReports,functions:recoverAccountDeletions
```

The seven-export historical translation-only scope is superseded because all 17 need the production-capable shared gate. No rules/indexes/Storage/media/messaging/business handler deployment. Existing four additive review indexes must be READY; if not, stop for separate scope approval. Account finalizer code changes immediately but uses unchanged manual permission/checkpoint flow; new recovery remains off. Two scheduled workers use authenticated IAM; never apply browser-callable IAM correction to them. If Firebase repeats the known public invoker grant rejection, only the explicitly approved browser services may have invoker IAM check disabled; preserve Auth/App Check/application gates and current ingress. Request approval for any newly necessary correction, especially new translatepublishedcustomerreview.

## Acceptance while gates closed — mandatory

* All scoped Functions ACTIVE with approved source and correct flags; verify actual translation account and EU source pin translate-eu.googleapis.com/europe-west1, no global fallback or SDK retries. Effective permissions resolved before provider selection.
* All17 normal browser OPTIONS/callable endpoints reachable; closed gates reject actions safely before review/cache writes. Auth failures are expected, infrastructure IAM403 is not a pass. Confirm new callable invoker configuration and unchanged App Check processing.
* Existing retention Scheduler is ENABLED with OIDC identity above and matching audience; latest inspected lastAttemptTime 2026-09-10T15:35:23.615335Z, status empty/no reported error. This proves configuration/latest scheduler status only, not cleanup. Inspect natural new runs for both workers, authenticated success and no maintenance/review writes while flags closed. Anonymous worker invocation must fail; do not run cleanup to test connectivity.
* Read-only index and data preflight; no synthetic production records or account deletion. Validate notification receipt from the existing test, monitor filters against sanitized existing logs and policy descriptors. Missing-series alerts cannot be certified until active worker emits first counts. Controlled alert failures/retries belong in emulator/isolated environment; no new notification test authorised.
* Vercel final commit/aliases match approved source; Services, /my-reviews, /admin/customer-reviews and /admin/customer-review-reports direct navigation/reload retain guards. Do not treat a disabled empty page as positive review/provider acceptance.

## Real-provider acceptance and controlled activation

Recommended economical route: deploy disabled source, establish actual runtime effective permissions read-only, then perform a separately approved synthetic EU adapter probe under that SAME runtime identity using existing keyless impersonation permissions only. If current access cannot impersonate it, stop; do not add Token Creator or create a temporary production Function/job without explicit approval. Bound the proposed probe to one English→Spanish request, at most74 synthetic characters, no retries, EU endpoint/europe-west1 only, stop on first failure, at mostUSD0.01 before tax/conversion. Send no account/business IDs, display names, private/pending text, headers or credentials into logs. Existing identity/project billing metadata necessarily accompanies the authenticated Google request. A successful adapter probe proves runtime-identity/provider access, not the deployed callable/cache integration. No new paid probe is currently authorised.

Full deployed handler→provider→cache→revision acceptance requires an eligible approved published review. Do not create dummy production businesses/reviews or alter statuses. If a legitimate consenting reviewer/business becomes available, obtain specific approval to submit/moderate that genuine review, translate English→Spanish once (bound chars/cost before calling), reload for cache/no second provider call and later verify revision invalidation only on a genuine intended edit. Otherwise an isolated project/test dataset is required for full cloud lifecycle coverage, OR Craig must explicitly accept this as pending first genuine review after launch, relying on emulator privacy/cache coverage plus same-identity provider proof. No claim of complete cloud E2E until it happens.

Only after all closed-gate checks pass and explicit activation approval:
1. Configure google_cloud provider only on translation callable; EU pin unchanged. Review gates stay closed during provider configuration.
2. Enable recovery on recoverAccountDeletions only; observe a natural authenticated execution, verify eligible-work counts/unchanged unauthorised records and bootstrap recovery completion metric. Actual authorised erasures may complete and are irreversible; do not create test erasures. Keep retention closed until its own preflight passes.
3. Enable retention only on sweepResolvedCustomerReviewReports, observe natural counts/eligible cleanup and bootstrap completion absence policy. No eligible records means zero-deletion completion, not positive deletion evidence. Pause on unexplained failures; never bypass expiry/status.
4. Enable CUSTOMER_REVIEWS_ENABLED on all17 review callables; fail-closed rollback on partial enablement. Check public list/auth rejection without writes, then enable VITE_CUSTOMER_REVIEWS_ENABLED only for Vercel production and build exact approved final source. Confirm aliases/mobile customer/admin guards and genuine acceptance if authorised. Preview remains off.
5. Monitor EU warnings, scheduler/completion/errors and queues when Craig is available. No response-time promise. Record first real lifecycle evidence separately.

Cloud acceptance can fail even if local tests pass. Do not proceed through a blocked stage on the strength of prior approval. Resources may incur Functions/Cloud Run builds/runtime, Scheduler, Firestore, Monitoring and Translation charges; the EUR30 budget covers Translation only. Credits are included in budget consumption. No notification resend, quota override or broad IAM grant belongs in this package.

## Rollback

Close website/review gates/provider selection, then recovery/retention flags to stop new work (in-flight work may complete). Restore captured prior website deployment and scoped Function artifacts/env/IAM settings only. Keep monitoring; if needed pause only newly affected authenticated schedules. Never publish worker endpoints or erase progress/cache as a workaround. Preserve checkpoint/progress records and unrelated indexes/settings. Remove only newly created exact IAM binding/alert resources if necessary and approved. Provider usage is billable even on rollback; completed erasures/retention deletions cannot be undone. No automatic quota or billing-budget rollback.

## Public disclosure integrated — website only

Your chosen review display name and approved review text are public. We keep an internal account link for moderation; your account name and email are not automatically used as your public review name. Published review text may be sent to Google Cloud Translation using its EU endpoint. Original text remains available and translations are labelled. Private or pending review text is not sent through this public translation service. A display name or verified email does not prove a purchase or service. Review reports are private; open reports are kept until handled, and resolved report text, reporter identity and private notes, including linked operational copies, are removed after90days, with earlier identifying-data cleanup on account erasure.


The existing English privacy content and lazy legal locales now render exactly one dated reviews section using shared disclosure copy for all17 languages. The authoritative AuthorForm displays the provider notice before submit/edit, associates it with the textarea and links to /privacy. Existing public-name, no-proof-of-purchase and moderation notices remain. No new checkbox, legal consent version, account-wide reacceptance, policy, endpoint or provider change. The added section is dated10September2026 separately from the existing document consent version1.0. An account-wide reacceptance requirement would need an explicit separate decision; it is not necessary to implement this approved clarification and has not been inferred.

Verified against provider request fields and review deletion/retention implementation: published text includes anything written into it; account IDs/names are not separate Translation fields; private/pending text is excluded from this public translation path; names/originals are preserved; scheduled90-day cleanup includes linked request/audit copies and earlier account erasure. The review-specific retention paragraph clarifies the general deletion section rather than changing other retained records. No guarantee of deletion at an exact instant, worldwide EU-only storage, purchase verification, or immunity from content-based personal data is made.

Affected verification:17 unit/component/locale tests pass; all17 languages at390/1440 browser widths using actual legal/form/router components with synthetic local state, keyboard privacy link, direct privacy navigation/reload, associated notice, no horizontal overflow or submission/provider calls; locale parity17, website lint and fresh default-off build pass. Translation wording is authored and checked for consistency; automated checks are not native-speaker editorial/legal review. No wording decision is needed for this approved-policy integration. Evidence: review-evidence/translation-budget/disclosure-*.log and disclosure-browser/result.json. The earlier backend358/36 emulator verification remains applicable. New preview must pass on final head before approval.
