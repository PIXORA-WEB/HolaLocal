# Retention backend preparation — disabled, not a release recommendation

Baseline main203895227b41c03b87e8edc4a33f57134055a4b8. Three separately reviewable backend batches, no production writes or cloud resources. Original mixed workspace, PR44 and backups preserved.

## Scope
1. Acknowledgments and exceptions: authoritative accountDeletionPrimitives plus shared trusted workflow contract. Completed deletions receive a due-now review record assigned to the existing finalizing admin, once. No historical review dates fabricated. Admin can explicitly record/release a specific need; cleanup defaults disabled and requires a released valid assessment, completed workflow and no surviving user profile. Deletes four-field acknowledgment evidence and its decision, not terminal workflow markers. Existing-account consent evidence and incomplete checkpoints remain untouched. Exception reasons/reviewer/reviewAt/end condition validated, optimistic revisions avoid lost decisions; overdue does not expire or renew a hold.
2. Business reports: first authoritative backend handling service for existing reports collection. Moderator/admin resolution changes the report only, with trusted resolvedAt and idempotency; no business approval/publication/maintenance operation. Admin-only exceptions.90days after resolution removes entire supported report record (personal text/identity/links included) rather than retain an unjustified summary. Open/legacy undated/unknown-copy schemas are not silently migrated. Review-report policy/code untouched.
3. Conversations: existing deletion primitives, private conversationRetention decision records (default client deny rules), actual Auth existence plus trusted finalisation/profile checks. No recent-login criterion; even disabled Auth account preserves history. Bounded message+embeddedtranslation/parentpreview removal only after both accounts gone and explicit needs assessment release. Existing/unknown attachments fail closed. Explicit admin-reviewed message erasure supports already-authorised account-deletion requests, preserves a tombstone preventing old sends resurrecting text, clears matching preview and translation, leaves other history. Private assessment never placed in participant-readable data. General standalone privacy requests still require their existing human process, not mandatory account deletion; no new UI for those cases is claimed.

## Verification
57 sequential targeted unit/regression checks passed (existing finalizer/recovery/admin projection, shared deletion contract, message retry and exception logic). Combined Auth/Firestore emulator suite8passed/0skipped. Additional exact private-rule boundary1passed: anonymous and actual authenticated participant REST reads403. That filtered run intentionally skipped3already-verified cases. The Auth emulator was used for account-existence checks; only explicit network-error case injected failure. Concurrent transaction tests observed emulator lock retries and completed successfully. Lint and diff checks passed. Vendor source bytes/integrity verified. No full fresh dependency install; reused modules with isolated shared contract symlink. No website changes/build/bundle change; known website budget failure remains untouched. No cloud Translation/cache tests repeated.

## Not yet production-ready
No callable/Scheduler exports or website controls added. These are backend preparation batches, not a functioning timed retention service. No public claim that cleanup runs. Next integration must provide authenticated admin controls to see/resolve due reviews and resolve business reports, a reviewed executor with scheduling/closed controls/monitoring, per-record failure isolation and cursor fairness. Interface contracts and browser checks, newly completed evidence review UX, stale/legacy assessment UX, whole-request operational metadata retention, late Auth identity recreation risk outside normal platform flows, attachment cleanup, and standalone message-erasure request recording need scoped completion before any destructive activation. No staffing commitments or automatic hold renewal.

## Deployment requirements
None authorised or recommended yet. Existing finalization metadata change would affect finalizeAccountDeletion and recoverAccountDeletions, with the packaged shared contract. New report/conversation/assessment helpers are NOT deployed endpoints; exact new exports, schedule and any indexes must be specified and approved after integration. Client rules unchanged; private conversationRetention remains denied to all clients by fallback rule. No business/media/Storage/translation handler changes.
PR44 is independent and may release first. Source merge analysis finds only generated contract archive/package-lock integrity conflicts; primitives merge automatically with PR44 historical-consent fix. Repack the combined authoritative shared source and verify integrity when integrating; never choose one old archive. Combined PR44+retention build/tests not yet claimed. Retention work is not imposed as a customer-review activation dependency: retain truthful current-state disclosures and keep review/provider/retention/recovery controls separately approved. Future promise of this new cleanup depends on its own enforcement acceptance, not repeated provider tests.

## Rollback/approval
Local source only. Keep published wording on current behaviour. Before later deployment record revisions and get concrete scope approval. Do not roll back to code that loses existing historical-version access or treats evidence removal as an incomplete deletion. No production deletion can be reversed by a source rollback; preserve hold/decision checks and require specific approval before activation.
