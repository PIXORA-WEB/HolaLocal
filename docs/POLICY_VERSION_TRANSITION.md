# Nonblocking policy transition — candidate, not published

Approved behavior: preserve valid historical acknowledgments and timestamps for access and account deletion; an informational notice is not agreement. New registrations record candidate1.1, while final publication/effective dates remain pending (null, never backdated). Draft pages remain explicitly not effective. Versions1.0 and1.1 are an explicit compatibility set; unknown/malformed evidence is rejected. Existing1.0 clients remain supported by rules. Accepted fields cannot be rewritten/downgraded by clients.

Authoritative shared validation separates hasValidLegalConsent (access) from hasCurrentLegalConsent (current-version evidence). Recording preserves independently valid Terms/Privacy records during partial recovery. New website callable payloads explicitly identify the displayed versions; legacy payloads without versions record1.0, never fabricated1.1 acceptance. No new analytics consent or existing-account age requirement.

Notice lives in existing SiteLayout using shared styles and original translation composition; non-English text stays lazy. Local dismissal only; no acceptance write, storage consent or acknowledgment timestamp. Effective-date-null suppresses notice before publication. Browser test simulates a future publication date only in its Vite test transform; not part of a production build.

Verification:74 targeted shared/backend/access tests plus callable-boundary coverage;98 Firestore/Storage emulator rules tests. Browser:34locale/viewport registration checks,8invalid age guard rejections,2real synthetic Auth/Firestore registrations/reloads storing1.1; historical1.0 login/profile reload/deletion request with original timestamps retained. Informational notice keyboard dismissal/reload verified with a synthetic publication date. Final lazy-notice path rechecked separately; no production data, provider calls or Analytics traffic. Local failed attempts were test setup: occupied default ports (preserved unrelated emulators), mismatched Admin SDK timestamp classes (aligned backend SDK), missing mobile shared-contract link (reused local source). No permissions weakened to fix them.

Added deployment scope, still requires final approval:
1. Firestore rules version compatibility only. No Storage rules/index changes. Existing1.0 registration remains valid during staged rollout.
2. Six Functions with regenerated shared-contract package: acceptLegalConsent, requestAccountDeletion, cancelAccountDeletion, finalizeAccountDeletion, listAdminAccountDeletionRequests, recoverAccountDeletions. Keep recovery/review/provider/retention controls closed; preserve IAM/invoker/ingress and dedicated translation identity. Account-deletion finalizer and admin listing use shared eligibility primitives; include them, not only requestAccountDeletion. Backend goes before the new website: explicit new payload fields would be rejected by the old callable. Legacy clients continue writing1.0 via compatibility defaults.
3. Only after publication particulars/date/final wording and exact-source approval: website publication. No notice silently binds old accounts to revised Terms. Material renewed agreement remains separately justified/approved.
4. Review/provider/retention/recovery activation remains a separate concrete approval; the older19configuration-only launch proposal is superseded by this added source-deployment prerequisite.

Rollback: never deploy old exact1.0 validators after1.1 registrations exist; that would block their accounts/deletion. Retain compatibility backend/rules as a bridge and disable the new notice or review gates as appropriate. Do not overwrite1.1 records, fake old timestamps, restore erased data or downgrade accepted evidence. Record source/revisions before the eventual rollout; stop on unapproved IAM correction. Final legal date/version wording remains an explicit release gate.
