# Translation monitoring update

Offline generator now covers17 review callables including translatePublishedCustomerReview, two count metrics and eight disabled policies. The new warning policy matches structured or text google_translation_failure records on only that service; safe unavailable responses need not produce HTTP5xx. No sensitive label extraction. Recipient is Craig at hello@holalocal.es; no fixed staffed hours or backup. The email destination is configured and one labelled test is authorised; operational policies remain offline. Cloud filter validation and delivery remain approval-gated.

# Reviews monitoring and activation readiness

Current main at preparation: `428cdbc08617d53e3498ffb5593e7200807dfa7c`. Website and disabled backend are deployed; PR32 routes and the16 invoker checks are verified. This is LOCAL preparation, not authorization to create resources, notify anyone or activate either gate. See PAUSED_WORK_STATUS.md for current backlog authority; earlier readiness deployment steps are historical.

## Ownership and local artifacts

Primary: Craig, hello@holalocal.es, Europe/Madrid. Checks when available while working full time. No fixed staffed hours, guaranteed response time or backup responder. Do not publish or infer a response-time promise. Email destination configuration and one clearly labelled delivery test are authorised; operational alert activation and spending limits are not. Channel resource is recorded in private durable monitoring-contact evidence.

Run from this isolated source:

```
node --test scripts/reviews-operations/prepare.test.mjs
node scripts/reviews-operations/prepare.mjs holalocal-491c9 /absolute/durable/output-directory
```

The generator has NO network, deployment or notification capability. It creates two log-metric and seven DISABLED alert-policy request bodies, with no channels by default. Optionally pass existing `projects/holalocal-491c9/notificationChannels/NUMERIC_ID` resources. Each policy stays disabled regardless. No environment file or credentials required. Local tests establish scope and defaults, not Cloud Monitoring API acceptance or actual alert delivery.

| Prepared alert | Trigger and response |
| --- | --- |
| Scheduler failure | Failed AttemptFinished: inspect authenticated target, worker and retry logs; do not change gates as a remedy. |
| Runtime error | ERROR on the16 review services, retention worker or finalizer. Check code/revisions; preserve resumable erasure. |
| Missing Scheduler success | Fewer than1 success in a rolling120min window for5min. Bootstrap a natural success metric before arming; an entirely absent/new series must not be assumed monitored. |
| Missing retention completion | Same window on count-only enabled-worker completion. MUST remain disabled until retention activation and first successful completion. Scheduler200 alone is insufficient. |
| Retention capacity | Early warning on pageLimitReached=true; inspect next run. Two consecutive full pages need capacity review. The generated alert is NOT a consecutive-run detector. |
| Callable server errors | At least5 HTTP5xx in5min across16 services; ERROR alert also catches low-volume incidents. Expected gate400/auth401 are excluded. |
| Callable latency | Per-revision p99 above10 seconds for5min. Verify metric unit milliseconds and interpret low-traffic results cautiously. |

Manual checks when Craig is available (not a promised daily schedule): open reports older than7days in the existing private admin queue; stuck/failed account-erasure jobs in their existing queue. Caught domain failures may not emit ERROR, so an error-log alert alone is not complete erasure monitoring. No new private-data projection, automatic report closure or moderation policy. Logs/metrics must not extract review IDs, text, reporter identity, emails, tokens or notes into labels or notification content. Alert filters use existing metadata and counts only.

## Exact cloud acceptance sequence (requires separate authorization where it creates resources)

1. **Read-only baseline:** recheck main/Vercel source and aliases; capture18 Function revisions/gates,16 callable invoker flags, Scheduler IAM/job and effective org policy. Pass: both gates closed,16 invoker checks disabled without allUsers, Scheduler authenticated and unchanged. Capture11 counts only; existing records do not imply migration or deletion permission.
2. **Read-only metric/filter readiness:** inspect metric descriptors `run.googleapis.com/request_count` (DELTA INT64, response_code_class label) and `run.googleapis.com/request_latencies` (DELTA distribution, milliseconds). Query each prepared Logging filter against a bounded recent window. Pass: Scheduler-success matches observed natural200; failures do not match successful/expected4xx fixtures; retention completion filter matches the actual structured logger shape in an isolated enabled run. No production errors need to be manufactured.
3. **After monitoring-resource approval:** create only the two named log metrics and seven disabled policies from the generated files; inspect server-normalized filters and disabled state. Reuse a verified existing notification channel or separately approve creating one. API creation is not a dry run. Do not enable retention-completion monitoring yet. No log exclusions, buckets or IAM changes included.
4. **Natural Scheduler acceptance:** allow a natural hourly execution, never Run now as a read-only step. Pass: AttemptFinished200 and matching Cloud Run Scheduler user-agent request, private worker auth unchanged; success counter appears within10min of log arrival. Gate remains closed and no completion/cleanup writes occur. If no series appears, stop: absence detection is not ready.
5. **Isolated monitoring acceptance, separately approved:** mirror filters/resources in a non-production project, run a synthetic scheduled handler producing success then failure, and allow a controlled120min success gap. Pass: failure incident opens, absence opens after window/retest/ingestion allowance (target <=140min), recovery closes; only intended project/services match. Test latency/error thresholds with synthetic traffic and both full-page and non-full-page completion logs. Verify actual notification delivery/acknowledgment only after recipient/test-message authorization. Local predicates or incident listings do not prove delivery.
6. **Retention acceptance in an isolated authorized environment:** create synthetic expired resolved report plus linked request/audit copies, unexpired resolved report, open report, and a newly submitted report cycle. Natural worker run removes only eligible expired copies; open/unexpired/new-cycle records remain; repeat is idempotent. At90days exactly expiry is eligible;89days23h59m remains. Test page boundary50 and backlog51 per collection across subsequent runs, concurrent new report/resolve changes, failures/retry, and account erasure earlier than90days. Logs contain only counts and the cap flag. Existing29 emulator checks are evidence for local logic, not real Scheduler/Firestore cloud verification. Do not seed production or erase production accounts.
7. **Review activation candidate:** current backend gate requires the protected demo emulator, and website rejects PROD even with the flag true. Environment flags alone CANNOT activate reviews. Prepare/review a separate gate change plus outstanding automatic review-text translation. Reuse released contracts, Auth/current-account checks, quotas, privacy and erasure. Retention gate differs: its true value WOULD enable cleanup, so never set it casually. No gate code changed here.
8. **Authorized isolated browser acceptance on that candidate:** verified synthetic customer chooses a public name; submit/edit quotas5 combined, report quota10 rolling24h; retry after dropped response creates one revision/report; free withdrawal; approval/rejection/removal and pending edit preserve approved content; unauthorized owners/non-admins denied; safe report business links; all17 language switches automatically translate published text and permit original view; translation cache invalidates on revision/unpublish/erasure. Account-erasure cleanup must also pass with reviews disabled. Fail any missing privacy, retention, translation or retry result.
9. **Production activation is a later approval:** verify ready indexes and current configs again; arm delivery/error monitoring after approved notification test; activate retention and verify completion before accepting reports; release backend gate capability then website access in coordinated order. No synthetic production records are part of this plan. Observe legitimate opted-in flows after activation. If authorized isolated cloud environment is unavailable, cloud mutation/notification checks stay explicitly blocked; emulator success is not substituted.

## Costs and rollback

No billable resources were created by this preparation. The existing hourly Scheduler job costs up to US$0.10/month, with3 free jobs per billing account; Functions/Cloud Run, Firestore operations and build/artifact storage remain usage-billed. [Scheduler pricing](https://cloud.google.com/scheduler/pricing)

As checked10September2026, Google lists alert-policy billing as starting **no sooner than1September2027**, then US$0.35 per metric reference/month plus US$0.50/million returned query points. User-defined log metrics consume the shared150MiB monthly free metric allotment; excess and Logging can be billed. These two low-cardinality hourly counters should be small, but account-wide usage determines the bill. Recheck pricing before creation; no fixed total or free-tier availability is guaranteed. [Observability pricing](https://cloud.google.com/products/observability/pricing)

A conservative future alert-reference estimate for7 single-condition policies is US$2.45/month plus query points, assuming the published pricing applies then; current incremental policy charge is not that future rate. Recommend a US$10/month incremental monitoring budget alert (not a hard spending cap), plus a separate usage budget for reviews/Firestore/translation. Budget creation/recipient is also approval-dependent. No new VM, paid external monitor, uptime probe, project or bucket is included in the local plan.

Automatic review translation is unfinished; reusing the existing Google NMT adapter would add translation API usage (currently US$20/million characters after applicable allowance, multiplied by requested target languages), caching reads/writes and privacy/provider disclosure work. No translation API activated or calls made. [Translation pricing](https://cloud.google.com/products/translate/pricing)

Monitoring rollback: disable only newly created policies by recorded resource IDs; keep delivery evidence and unrelated monitoring intact. Review rollback after real data exists: close public access/writes, retain compatible reads, erasure and90-day cleanup. Never restore synthetic quotas or remove active indexes. Transport rollback: only16 `--invoker-iam-check` commands from the approved IAM release record restore the old private boundary; Scheduler excluded. Firebase CLI14.27.0 updates are expected to preserve the service flag, but creation repeats the rejected public grant. Check flag/policy/endpoint drift after every deployment.

## Notification restraint

The offline plan uses24-hour log-alert notification limits, no configured renotification intervals, and no notification channels on overlapping callable5xx/latency diagnostic policies. Runtime-error coverage excludes the retention worker because its failed execution is covered by Scheduler failure; semantic account-erasure failures still require a dedicated automated state check. These settings reduce duplicate emails, but Cloud Monitoring does not provide global cross-policy root-cause deduplication here. Operational policies are not enabled. Reassess separate critical incidents during cooldown when configuring; do not promise exactly-once email delivery. The one authorised test uses one unique event and a temporary policy, with no resend.
